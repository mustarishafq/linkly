# Environment variables are split by app:
#
#   backend/.env.example  — Laravel API (database, JWT, SMTP, admin)
#   frontend/.env.example — Vite frontend (VITE_* only)
#
# Copy each to .env in its folder before running locally.
