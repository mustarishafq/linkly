# Copy to frontend/.env.production before `npm run build` for production deploy.
# Vite loads .env.production automatically when building (vite build).

VITE_APP_TIMEZONE=UTC
VITE_API_BASE_URL=https://linklyapi.emzinexus.com/api
VITE_NEXUS_BRAIN_URL=https://emzinexus.com
